{
  "name": "snail-runner-prototype",
  "version": "0.3.0",
  "private": true,
  "type": "module",
  "scripts": {
    "start": "npx serve .",
    "test": "node --import ./test/setup.mjs --test \"test/**/*.test.js\""
  }
}
